<UserControl x:Class="PL.Volunteer.VolunteerMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL.Volunteer"
             Loaded="Window_Loaded"
             DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="110"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Background="#284151" Panel.ZIndex="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="295"/>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="330*"/>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="55"/>
                <ColumnDefinition Width="71*"/>
            </Grid.ColumnDefinitions>
            <Grid.Effect>
                <DropShadowEffect Opacity="0.25" BlurRadius="10" ShadowDepth="7.3" Direction="-90"/>
            </Grid.Effect>
            <TextBlock Text="WeirdAid" FontSize="48" Foreground="White" FontWeight="Regular" TextAlignment="Center" Width="268" HorizontalAlignment="Center" Height="64" Grid.Column="1" />

            <Border Background="#FF7690A3" Grid.Column="3" CornerRadius="16" MinWidth="300" MaxWidth="500" MaxHeight="55">
                <Grid>
                    <RadioButton Content="Open calls" GroupName="A" Style="{StaticResource HeaderRadioButtonStyle}" Click="CurrentViewToOpenCallsInList" HorizontalAlignment="Left" Width="140" Margin="50,0,0,0" Height="40" IsEnabled="{Binding IsNotCall}"/>
                    <RadioButton Content="History" GroupName="A" Style="{StaticResource HeaderRadioButtonStyle}" Click="CurrentViewToCallHistory" Height="40" Width="140" Margin="0,7,50,7" HorizontalAlignment="Right"/>
                </Grid>
            </Border>
            <Grid Grid.Column="7">
                <Label Content="{Binding Initials}" Style="{StaticResource RoundedLabel}"  FontSize="26" VerticalAlignment="Center" HorizontalAlignment="Center" Width="55" Height="55" Foreground="#FF7690A3" Background="White"/>
            </Grid>
            <Grid Grid.Column="5">
                <RadioButton Content="Menu" GroupName="A" Style="{StaticResource HeaderRadioButtonStyle}" FontSize="26" Click="CurrentViewToMenu" VerticalAlignment="Center" HorizontalAlignment="Center" Width="80" Height="55" IsChecked="True"/>
            </Grid>
        </Grid>

        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</UserControl>
