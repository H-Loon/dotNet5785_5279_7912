<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded" Closed="Window_Closed"
        Title="WeirdAid" Height="1080" Width="1920" MinWidth="1300" MinHeight="920">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="110"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.Background>
            <RadialGradientBrush Center="0.5,0" GradientOrigin="0.5,0">
                <GradientStop Color="#87A6BC" Offset="0"/>
                <GradientStop Color="#CC223D4E" Offset="1"/>
            </RadialGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!-- Header -->
        <Grid Background="#284151" Panel.ZIndex="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="86*"/>
                <ColumnDefinition Width="295"/>
                <ColumnDefinition Width="114*"/>
                <ColumnDefinition Width="915*"/>
                <ColumnDefinition Width="255*"/>
                <ColumnDefinition Width="158"/>
                <ColumnDefinition Width="97*"/>
            </Grid.ColumnDefinitions>
            <Grid.Effect>
                <DropShadowEffect Opacity="0.25" BlurRadius="10" ShadowDepth="7.3" Direction="-90"/>
            </Grid.Effect>
            <TextBlock Text="WeirdAid" FontSize="48" Foreground="White" FontWeight="Regular" TextAlignment="Center" Width="268" HorizontalAlignment="Left" VerticalAlignment="Top" Height="64" Margin="0,28,0,0" Grid.Column="1" />
            <!-- Header Button -->
            <Border Margin="0,28,0,28" Background="#FF7690A3" Grid.Column="3" CornerRadius="16" MinWidth="500">
                <Grid MinWidth="500">
                    <RadioButton Content="Volunteers" Style="{StaticResource HeaderRadioButtonStyle}" Click="CurrentViewToVolunteerInList" HorizontalAlignment="Left" Width="140" Margin="20,0,0,0" Height="40"/>
                    <RadioButton Content="Calls" Style="{StaticResource HeaderRadioButtonStyle}" HorizontalAlignment="Center" Width="64" Height="40"/>
                    <RadioButton Content="Assignments" Style="{StaticResource HeaderRadioButtonStyle}" HorizontalAlignment="Right" Width="160" Margin="0,0,20,0" Height="40"/>
                </Grid>
            </Border>
            <Grid Grid.Column="5" Margin="11,18,34,17">
                <Ellipse Fill="#FF7690A3" Height="75" Width="75"/>
                <Button Content="EH" FontSize="36" Click="CurrentViewToAdmin" VerticalAlignment="Center" HorizontalAlignment="Center" Width="51" Height="49"/>
            </Grid>
        </Grid>

        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
